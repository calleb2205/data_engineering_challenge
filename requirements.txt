apache-airflow-providers-postgres==5.11.1
# pandas>=2.2 exige sqlalchemy>=2.0 pra reconhecer um Engine em to_sql(), mas o Airflow 2.9.3
# exige sqlalchemy<2.0 -- por isso pandas fica travado em 2.1.x (última versão compatível com
# sqlalchemy 1.4.x) para não quebrar df.to_sql() na task load_raw.
pandas==2.1.4
pyarrow==16.1.0
sqlalchemy==1.4.52
psycopg2-binary==2.9.9
